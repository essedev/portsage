[project]
name = "portsage-mcp"
version = "0.1.0"
description = "MCP server for Portsage - port allocation manager"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[project.scripts]
portsage-mcp = "server:main"
